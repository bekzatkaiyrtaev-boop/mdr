-- ============================================================
-- Миграция 015 — колонка "Ответственный" у конкретного альбома
-- (position_disciplines) — кто отвечает именно за этот альбом в этой
-- позиции (может отличаться у разных альбомов одного и того же раздела,
-- напр. КЖ1 и КЖ2 у разных инженеров). Свободный текст, как marker/note.
-- Выполнить в Supabase → SQL Editor на уже созданном проекте
-- ============================================================

alter table public.position_disciplines add column if not exists responsible text;
