-- ============================================================
-- Миграция 006 — все авторизованные пользователи видят имена друг друга
-- (нужно для колонки "Разработал" в MDR — иначе видно только своё имя)
-- По решению пользователя: доступ на чтение профилей открыт всем,
-- на изменение — по-прежнему только себе или ГИП (не меняется).
-- Выполнить в Supabase → SQL Editor на уже созданном проекте
-- ============================================================

drop policy if exists "profiles_select" on public.profiles;
create policy "profiles_select" on public.profiles for select using (true);
