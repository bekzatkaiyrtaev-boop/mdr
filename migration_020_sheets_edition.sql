-- ============================================================
-- Миграция 020 — колонка "Редакция" у листа (public.sheets.edition).
--
-- Отдельное поле от уже существующего sheets.revision ("Ревизия" — задаётся
-- только во вкладке "Состав разделов", в MDR лишь отображается). "Редакция"
-- задаётся прямо в MDR, стандартные значения — R01, R02, A1, A2, A3.
-- Выполнить в Supabase → SQL Editor на уже созданном проекте
-- ============================================================

alter table public.sheets add column if not exists edition text;
