-- ============================================================
-- Миграция 022 — запрет на два раздела с одинаковым кодом.
--
-- Без этого ограничения опечатка в коде раздела (совпавшая с кодом уже
-- существующего раздела) молча "сливала" альбомы одного раздела с другим —
-- именно так коды/названия расходились местами.
--
-- ВАЖНО: сначала выполните шаг 1 (проверка) — если он что-то вернёт,
-- нужно вручную поправить код у одного из задвоенных разделов во вкладке
-- "Проект" в приложении, и только потом выполнять шаг 2 (само ограничение),
-- иначе ALTER TABLE не пройдёт.
--
-- Выполнить в Supabase → SQL Editor на уже созданном проекте
-- ============================================================

-- 1) проверка — есть ли сейчас задвоенные коды
select code, array_agg(name_ru) as names, count(*) as cnt
from public.disciplines
where code is not null
group by project_id, code
having count(*) > 1;

-- 2) если предыдущий запрос вернул 0 строк — выполните это:
alter table public.disciplines
  add constraint disciplines_project_code_unique unique (project_id, code);
