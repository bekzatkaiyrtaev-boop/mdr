-- ============================================================
-- Миграция 025 — год заключения договора для титульного листа.
--
-- На титуле город/год должны показывать год НА МОМЕНТ ЗАКЛЮЧЕНИЯ ДОГОВОРА,
-- а не текущий год печати — раньше подставлялся new Date().getFullYear().
--
-- Выполнить в Supabase → SQL Editor на уже созданном проекте
-- ============================================================

alter table public.projects
  add column if not exists contract_year text;
