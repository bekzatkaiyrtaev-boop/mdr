-- ============================================================
-- Миграция 007a — только «№ п.п.» (номер тома/книги) у раздела,
-- без исполнителей (это отдельно, migration_007_multi_assignees_volume.sql,
-- выполнить позже, когда дойдёт очередь до прав доступа по разделам)
-- Выполнить в Supabase → SQL Editor на уже созданном проекте
-- ============================================================

alter table public.disciplines add column volume_number text;
