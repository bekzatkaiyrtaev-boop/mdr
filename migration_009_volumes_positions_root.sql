-- ============================================================
-- Миграция 009 — флаг "в этот том вкладываются позиции по генплану"
-- Сами 5 томов по умолчанию создаются автоматически из приложения
-- (при первом открытии вкладки MDR, если томов ещё нет).
-- Выполнить в Supabase → SQL Editor на уже созданном проекте
-- ============================================================

alter table public.volumes add column if not exists is_positions_root boolean not null default false;
